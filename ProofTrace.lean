import ProofTrace.Extraction

/-!
Public façade for the structured, kernel-checked proof-trace extractor.

Implementation is split into `ProofTrace.Schema`, `ProofTrace.Dependencies`, and
`ProofTrace.Extraction`; existing `import ProofTrace` callers remain unchanged.
-/
